{
  "claimType": "redis",
  "displayName": "Redis ElastiCache Instance",
  "defaultSecretIdentifier": "session-redis",
  "parameters": [
    {
      "name": "size",
      "label": "Approved Size Tier",
      "type": "select",
      "options": ["small", "medium"],
      "default": "small"
    }
  ]
}
