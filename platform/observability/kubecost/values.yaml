# Fresh installation of kubecost/kubecost 3.2.4. The 3.x FinOps agent
# collects metrics directly; the retired 2.x Prometheus URL is not consumed.
global:
  clusterId: idp-prod
  defaultStorageClass: gp3
frontend:
  service:
    type: ClusterIP
networkCosts:
  enabled: false
