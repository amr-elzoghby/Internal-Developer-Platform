apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: default
  namespace: argocd
spec:
  description: Default project is intentionally closed; every application must use an approved project.
  sourceRepos: []
  destinations: []
  namespaceResourceBlacklist:
    - group: '*'
      kind: '*'
  clusterResourceBlacklist:
    - group: '*'
      kind: '*'
