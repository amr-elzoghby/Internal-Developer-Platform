apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: serverinstances.idp.io
spec:
  scope: Namespaced
  defaultCompositionUpdatePolicy: Manual
  enforcedCompositionRef:
    name: serverinstances.idp.io
  group: idp.io
  names:
    kind: ServerInstance
    plural: serverinstances
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                owner:
                  type: string
                  description: Accountable tenant team; admission requires the namespace owner.
                  enum: [identity-platform, platform-engineering, data-platform]
                expiresOn:
                  type: string
                  format: date
                  description: Review-by date in YYYY-MM-DD; expiry never deletes cloud resources.
                size:
                  type: string
                  default: small
                  enum:
                    - small
                    - medium
                image:
                  type: string
                  description: Platform-reviewed AL2023 image from the pinned Terraform catalog.
                  default: al2023-ssm
                  enum: [al2023-ssm]
              required:
                - owner
                - expiresOn
                - size
                - image
